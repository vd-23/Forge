name: Forge

fileGroups:
  - docs
  - project.yml
  - Brewfile

options:
  bundleIdPrefix: com.forge
  deploymentTarget:
    iOS: "26.1"
  createIntermediateGroups: true
  generateEmptyDirectories: true

packages:
  ForgeCore:
    path: Packages/ForgeCore

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    CODE_SIGN_STYLE: Automatic
    # Free personal team: profiles expire after 7 days, and no entitlement that
    # needs the paid programme (App Groups, push) can be provisioned.
    DEVELOPMENT_TEAM: YLDXG3RWUU
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  Forge:
    type: application
    platform: iOS
    sources:
      - path: Forge
    dependencies:
      - package: ForgeCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.forge.gym
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: YES
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES

  ForgeTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: ForgeTests
    dependencies:
      - target: Forge
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.forge.gym.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  Forge:
    build:
      targets:
        Forge: all
        ForgeTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ForgeTests
